{% extends 'deelnemer.html.twig' %}
{% block menu %}
    <li><a href="{{ path('activiteiten') }}">home</a></li>
    <li><a href="{{ path('account')}}">account</a></li>
{% endblock %}


{% block content %}
    <section >
        <table class="table" style="table-layout: fixed">
            <thead>
            <tr>
                <td>naam</td>
                <td>achternaam</td>
                <td>username</td>
                <td>email</td>
            </tr>
            </thead>
            <tbody>
                    <tr>
                        <td>
                            {{ user.naam}}
                        </td>
                        <td>
                            {{ user.achternaam}}
                        </td>

                        <td>
                            {{ user.username}}
                        </td>
                        <td>
                            {{ user.email}}
                        </td>
                        <td>
                            <a href="{{ path('accountupdate')}}">edit account</a>
                        </td>
                    </tr>
            </tbody>
        </table>
        <br />
    </section>

{% endblock %}
